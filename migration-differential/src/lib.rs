//! Test-only crate. All the content is in `tests/differential.rs`; this lib
//! exists so the crate is a workspace member `cargo test --workspace` reaches.
//!
//! See `ui/src/migration_differential.rs` for the other half and for why the
//! differential is split in two.
